{
  "name": "cores-sorteio-api",
  "version": "1.0.0",
  "description": "API simples para salvar e carregar cores usando JSONBin ou fallback local.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
